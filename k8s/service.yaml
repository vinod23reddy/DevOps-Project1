apiVersion: v1
kind: Service
metadata:
  name: food-zone-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: food-zone-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080